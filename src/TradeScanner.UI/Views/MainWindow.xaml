<Window x:Class="TradeScanner.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Day Trade Scanner Pro"
        Height="800" Width="1280"
        MinHeight="600" MinWidth="900"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,1,0">
            <DockPanel>
                <!-- Logo -->
                <Border DockPanel.Dock="Top" Padding="16,20,16,16"
                        BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Text="TradeScanner" FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Day Trade Pro" FontSize="11"
                                   Foreground="{StaticResource MutedTextBrush}"/>
                    </StackPanel>
                </Border>

                <!-- Nav Items -->
                <StackPanel DockPanel.Dock="Top" Margin="0,8,0,0">
                    <Button x:Name="NavDashboard" Content="Dashboard"
                            Style="{StaticResource NavButtonStyle}"
                            Click="OnNavDashboard"/>
                    <Button x:Name="NavWatchlist" Content="Watchlist"
                            Style="{StaticResource NavButtonStyle}"
                            Click="OnNavWatchlist"/>
                    <Button x:Name="NavCharts" Content="Charts"
                            Style="{StaticResource NavButtonStyle}"
                            Click="OnNavCharts"/>
                    <Button x:Name="NavAlerts" Content="Alerts"
                            Style="{StaticResource NavButtonStyle}"
                            Click="OnNavAlerts"/>
                    <Button x:Name="NavProviders" Content="Data Providers"
                            Style="{StaticResource NavButtonStyle}"
                            Click="OnNavProviders"/>
                    <Button x:Name="NavSettings" Content="Settings"
                            Style="{StaticResource NavButtonStyle}"
                            Click="OnNavSettings"/>
                </StackPanel>

                <!-- Version at bottom -->
                <TextBlock DockPanel.Dock="Bottom" Text="v1.0.0"
                           Foreground="{StaticResource MutedTextBrush}"
                           FontSize="11" Margin="16,8" VerticalAlignment="Bottom"/>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Frame Grid.Column="1" x:Name="MainFrame"
               NavigationUIVisibility="Hidden"
               Background="{StaticResource BackgroundBrush}"/>
    </Grid>
</Window>
